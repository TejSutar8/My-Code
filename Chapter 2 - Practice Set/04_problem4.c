// 4. Explain step by step evaluation of
// 3*x/y – z+k, where x=2, y=3, z=3, k=1

#include <stdio.h>

int main()
{
    int x = 2, y = 3, z = 3, k = 1;

    float a = 3 * x / y;
    float b = z + k;

    printf("the value is and the ans is %f",a-b);

    return 0;
}